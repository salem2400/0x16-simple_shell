#include "main.h"
/**
 * my_exit - exit from program
 * @argument: have the commend
 *
 * Return: 0 in success
 */
void my_exit(int status)
{
	if (stutus)
	{
		return (atoi(status));
	}
	else
	{
		exit(status);
	}
}
