#include "main.h"
/**
 * my_exit - exit from program
 * @argument: have the commend
 *
 * Return: 0 in success
 */
int my_exit(int status)
{
	if (stutus)
	{
		return (atoi(status));
	}
	else
	{
		return (0);
	}
}
